# Design decisions index

Bootstrapped by plan item `G2` (`knowledge-base-research-2026-07-23`). This
directory holds designer decision-docs — copy registers, terminology locks,
visual-treatment contracts consumed by frontend-dev/backend-dev during
implementation. Docs here are the source of truth for the copy/labels they
cover; implementers apply them, they don't re-decide them inline.

## Docs

- [`kb-copy-register.md`](kb-copy-register.md) — Knowledge Base feature:
  "organizer" role terminology lock, `/kb` view copy register (group vs. DM),
  `/remember` copy, admin-panel KB entry + `kb_enabled` toggle labels.
  Gates plan item `A4`.
